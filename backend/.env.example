# Environment
NODE_ENV=development

# Server
PORT=3000
HOST=0.0.0.0

# Security (generate your own random string)
SESSION_SECRET=a3f5e9c1b4d7f0e2a9c8b7e6d5c4a3f2e1d0c9b8a7f6e5d4c3b2a1f0e9d8
# how to?
# in the terminal run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# copt the secret from the terminal and paste it here

# Database
DB_PATH=./users.db

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://localhost:8443
